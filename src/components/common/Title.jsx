export const Title77 = ({ className, children }) => {
  return (
    <h1
      style={{
        textShadow: "-8px 6px 8px #5F8792",
      }}
      className={`${className} text-[77px]  font-bold text-white`}
    >
      {children}
    </h1>
  );
};
export const Title32 = ({ className, title, children }) => {
  return (
    <h1 className={`${className} text-[32px]  font-semibold text-white`}>
      {children}
    </h1>
  );
};
export const Title97 = ({ className, children }) => {
  return (
    <h1
      className={`${className} text-[97px] font-bold text-[#6A594D]`}
      style={{ textShadow: "-7px 3px 4px rgba(70, 65, 59, 0.3)" }}
    >
      {children}
    </h1>
  );
};

export const Title20 = ({ className, children }) => {
  return (
    <h1 className={`${className} text-[20px]  font-medium text-secondaryColor`}>
      {children}
    </h1>
  );
};
export const Title16 = ({ className, children }) => {
  return (
    <h1 className={`${className} text-[16px]  font-normal text-secondaryColor`}>
      {children}
    </h1>
  );
};
